<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SI-JTI - Sistem Informasi Surat</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('assets/img/jti-bg.jpg');
            background-size: cover;
            background-attachment: fixed;
            height: 100vh;
            display: flex;
            align-items: center;
        }
        .auth-card {
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }
        .btn-primary { background-color: #00a0f0; border: none; border-radius: 10px; }
        .btn-secondary { background-color: #8a8a8a; border: none; border-radius: 10px; }
        .form-control { background-color: #e0e0e0; border: none; border-radius: 10px; padding: 12px; }
    </style>
</head>
<body>